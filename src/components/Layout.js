import { Outlet, Link } from "react-router-dom";
import './mainpage.css';

const Layout = () => {
  return (
    <>
      <nav>
        <ul>
          
          
        </ul>
      </nav>

      <Outlet />
    </>
  )
};

export default Layout;